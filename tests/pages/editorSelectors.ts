/**
 * Selectores alineados con qai-pa-pdf-editor `src/pages/editor/elements.json`
 * y `home/elements.json` (flujo Direct + upload).
 *
 * Valores canónicos: [`editor/elements.json`](./editor/elements.json) (mismo patrón POM que Selenium).
 */
import editorBundle from './editor/elements.json'

export const editor = editorBundle.editor
export const home = editorBundle.home
